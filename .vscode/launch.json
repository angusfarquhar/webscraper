{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(gdb) Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceRoot}/main.o", // Path to your executable, DONT INCLUDE ARGS
            "args": ["http://web1.comp30023"], // Arguments here as strings, seperated: ["arg1", "arg2"]
            "cwd": "${workspaceFolder}",
            "MIMode": "lldb",
    }
    ]
}